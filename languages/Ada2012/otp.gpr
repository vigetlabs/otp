project Otp is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("decrypt.adb", "encrypt.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-gnato", "-gnata", "-gnat12");
   end Compiler;

end Otp;

